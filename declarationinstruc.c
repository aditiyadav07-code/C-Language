#include <stdio.h>

int main()
{
    int a=18;
    int b=a;
    int c=b+2;
    printf("The value of a, b, c is: %d, %d, %d", a,b,c);

    int cd;
    int ab=cd=2007;
    printf("\nThe value of ab, cd is: %d, %d", ab, cd);

    int be=100, de;
    de=200;
    printf("\nThe value of be and de: %d, %d", be, de);

    // hello
    // hows it going bud       Use ctrl + / for multiple comments
    // doing well enough?
    return 0;
}